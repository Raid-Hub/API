export interface RaidHubLocals {
    // populated by region-metrics
    _region: string
    _asn: string
    _continent: string
    // populated by duration-metrics
    _startTime: number
    _duration: number
}
